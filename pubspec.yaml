name: dart_mappable
description: Serialize / deserialize dart objects effortlessly, without any boilerplate code or compromises.
version: 0.8.3
repository: https://github.com/schultek/dart_mappable
issue_tracker: https://github.com/schultek/dart_mappable/issues

dependencies:
  analyzer: ^2.2.0
  merging_builder: ^0.2.2
  build: ^2.1.0
  collection: ^1.15.0
  path: ^1.8.0
  source_gen: ^1.1.0
  code_builder: ^4.1.0
  equatable: ^2.0.3

dev_dependencies:
  build_runner: ^2.1.2
  lint: ^1.5.3
  test: ^1.17.4

dependency_overrides:
  analyzer: 2.2.0

environment:
  sdk: '>=2.12.0 <3.0.0'
